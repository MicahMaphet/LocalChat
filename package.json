{
  "name": "localchat",
  "version": "1.0.0",
  "description": "Chat room that runs a server on a localy.\r The server is programmed in Node.js and the database is in Mongodb.\r All the messages are saved in MongoDB and sent to the clients on connection.",
  "main": "index.js",
  "scripts": {
    "start": "npm run tsc && node build/src/server.js",
    "clean": "rm -rf build",
    "compile": "npm run tsc",
    "tsc": "npm run clean && tsc",
    "test:connect": "node build/test/connect.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/lodash": "^4.17.7",
    "@types/mongodb": "^4.0.7",
    "@types/node": "^20.14.11",
    "nodemon": "^3.1.4",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/socket.io": "^3.0.2",
    "express": "^4.19.2",
    "lodash": "^4.17.21",
    "mongodb": "^6.8.0",
    "socket.io": "^4.7.5"
  }
}
